name: CI Relative Path Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compile Expert
        shell: cmd
        run: |
          echo === Start compile ===
          "C:\Program Files\MetaTrader 5\metaeditor64.exe" /compile:"${{ github.workspace }}\MQL5\Experts\Aegis\SuhabFiboTrade\SuhabFiboTrader_v5674.mq5" /log:"${{ github.workspace }}\compile.log"
          type "${{ github.workspace }}\compile.log" || echo "⚠ Лог не найден"
