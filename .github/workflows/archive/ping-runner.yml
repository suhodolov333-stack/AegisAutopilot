name: Ping self-hosted runner

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: [self-hosted, aegis-mt5]
    steps:
      - name: Show runner info
        run: |
          echo USER=%USERNAME%
          echo COMPUTER=%COMPUTERNAME%
          ver
          whoami
